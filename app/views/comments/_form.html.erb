<%= form_with(model: [@publication, Comment.new], local: true) do |form| %>
  <% if form.object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(form.object.errors.count, "erreur") %> empêche(nt) la sauvegarde de ce commentaire :</h2>
      <ul>
        <% form.object.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :content, 'Ajouter un commentaire' %>
    <%= form.text_area :content, class: 'form-control', rows: 4 %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
